name: Manual Sky Harbor render verification

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-live-render:
    runs-on: ubuntu-latest
    steps:
      - name: Explain authoritative release gate
        run: echo "The authoritative Sky Harbor browser render verification now runs inside deploy-pages.yml and blocks release success."
