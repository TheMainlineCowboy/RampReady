name: PR 140 exact jetway acceptance

on:
  pull_request:
    branches:
      - maintenance/pr140-acceptance-base
    types: [opened, synchronize, reopened]
    paths:
      - .pr140-acceptance-trigger
  push:
    branches:
      - maintenance/pr140-acceptance-run
    paths:
      - .pr140-acceptance-trigger
      - .github/workflows/pr140-exact-acceptance.yml
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  statuses: write

concurrency:
  group: pr140-exact-jetway-acceptance
  cancel-in-progress: true

jobs:
  verify-and-render:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Resolve exact current PR 140 head
        id: pr
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          sha="$(gh api repos/${GITHUB_REPOSITORY}/pulls/140 --jq .head.sha)"
          test -n "${sha}"
          echo "sha=${sha}" >> "${GITHUB_OUTPUT}"
          echo "Exact PR 140 head: ${sha}"

      - name: Publish exact-head acceptance pending
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ steps.pr.outputs.sha }}
        run: |
          gh api --method POST "repos/${GITHUB_REPOSITORY}/statuses/${SHA}" \
            -f state=pending \
            -f context='PR140 exact visual acceptance' \
            -f description='Exact-head verification and visual render are running' \
            -f target_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"

      - name: Check out exact PR 140 head
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.pr.outputs.sha }}
          fetch-depth: 0

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Verify exact supplied jetway assets and repair contract
        shell: bash
        run: |
          set -euo pipefail
          node scripts/verify-a1-measured-ground-contact-contract.mjs
          node scripts/materialize-exact-airport-jetway.mjs
          node scripts/verify-exact-airport-jetway-runtime.mjs
          node scripts/verify-uploaded-airport-jetway-fleet.mjs
          node scripts/verify-uploaded-airport-jetway-articulation-v10.mjs

      - name: Run complete RampReady verification
        run: npm run verify

      - name: Build exact production artifact
        run: npm run build

      - name: Fail closed on stale or incomplete generated A1 evidence
        shell: bash
        run: |
          set -euo pipefail
          stale='terminal-relocated-a1-exact-cab-registration-v1'
          final='measured-a1-cab-inspection-pose-persisted-across-mode-toggle-v2'
          if grep -R --fixed-strings --line-number "${stale}" dist; then
            echo "The production bundle still contains the superseded A1 aircraft pose authority."
            exit 1
          fi
          for forbidden in \
            'named-landing-gear-wheel-bounds-v1' \
            'grounded-aircraft-wheel-contact-progressive-tunnel-slope-v2' \
            'grounded-aircraft-door-progressive-tunnel-slope-v1' \
            'exact-T4_WALK-A1-terminal-portal-v25' \
            'exactWalkwayPortalX' \
            'exactA1CabContactY += appliedA1JetwayVerticalFitMeters'; do
            if grep -R --fixed-strings --line-number "${forbidden}" dist; then
              echo "The production bundle still contains forbidden A1 walkway, child-lift, or stale grounding behavior: ${forbidden}."
              exit 1
            fi
          done
          grep -R --fixed-strings --line-number "${final}" dist
          grep -R --fixed-strings --line-number 'grounded-jetway-door-gap-reported-no-child-lift-v1' dist
          grep -R --fixed-strings --line-number 'inspectionAircraftDoorSignedVerticalGapMeters' dist
          grep -R --fixed-strings --line-number 'inspectionAircraftJetwayRequestedVerticalFitMeters' dist
          grep -R --fixed-strings --line-number 'inspectionAircraftJetwayVerticalFitMeters' dist
          grep -R --fixed-strings --line-number 'inspectionAircraftJetwayAuthoredBogieGroundPreserved' dist
          grep -R --fixed-strings --line-number 'authored-crj-lowest-geometry-contact-clusters-v2' dist
          grep -R --fixed-strings --line-number 'exact-authored-a1-lowest-geometry-ramp-contact-v1' dist
          grep -R --fixed-strings --line-number 'terminal4UploadedJetwayBogieGroundContactPointCount' dist
          grep -R --fixed-strings --line-number 'terminal4UploadedJetwayBogieGroundContactClusterCount' dist
          grep -R --fixed-strings --line-number 'terminal4UploadedJetwayBogieGroundHorizontalContactSpanMeters' dist
          grep -R --fixed-strings --line-number 'same-day-a1-continuous-compact-solid-closed-grounded-v1' dist
          grep -R --fixed-strings --line-number 'final-a1-acceptance-authority-after-all-preparers-v1' dist
          echo "Verified compact real-wall attachment, continuous closed vestibule, separated multi-point ramp contact, zero attached child lift, retained signed door gap, and final exact-head telemetry."

      - name: Install browser verifier and Chromium
        run: |
          npm install --no-save @playwright/test@1.55.0
          npx playwright install --with-deps chromium

      - name: Preflight exact A1 connection telemetry
        run: >-
          npx playwright test
          tests/browser/uploaded-jetway-articulation-v10.spec.js
          --reporter=line --workers=1

      - name: Render and verify A1, articulation, Terminal 4 and full airport
        run: >-
          npx playwright test
          tests/browser/a1-jetway-contact-clusters.spec.js
          tests/browser/a1-ground-contact-evidence.spec.js
          tests/browser/source-first-a1-repair.spec.js
          tests/browser/uploaded-jetway-articulation-v10.spec.js
          tests/browser/kphx-ground-runtime.spec.js
          tests/browser/full-airport-inspection.spec.js
          --reporter=line --workers=1

      - name: Upload exact current-head visual evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pr140-exact-acceptance-${{ steps.pr.outputs.sha }}
          path: |
            test-results/
            playwright-report/
          if-no-files-found: warn
          retention-days: 1

      - name: Publish exact-head acceptance success
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ steps.pr.outputs.sha }}
        run: |
          gh api --method POST "repos/${GITHUB_REPOSITORY}/statuses/${SHA}" \
            -f state=success \
            -f context='PR140 exact visual acceptance' \
            -f description='Exact-head contracts, build and browser evidence passed' \
            -f target_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"

      - name: Publish exact-head acceptance failure
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ steps.pr.outputs.sha }}
        run: |
          test -n "${SHA}" || exit 0
          gh api --method POST "repos/${GITHUB_REPOSITORY}/statuses/${SHA}" \
            -f state=failure \
            -f context='PR140 exact visual acceptance' \
            -f description='Exact-head acceptance failed; inspect workflow logs and retained evidence' \
            -f target_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
