import fs from "node:fs";

const sourcePath = "src/environment/sourceRegisteredA1RotundaElbowV3.js";
const PRIOR_AUTHORITY = "a1-real-photo-remote-rotunda-fixed-corridor-v1";
const AUTHORITY = "a1-aug15-photo-genuinely-remote-rotunda-placement-v2";
const SOURCE_REACH_AUTHORITY = "a1-aug15-photo-exact-supplied-connected-reach-v1";
const MIN_DIRECT_WALL_DISTANCE_METERS = 18;
const MAX_DIRECT_WALL_DISTANCE_METERS = 30;
const MIN_BRIDGE_TARGET_DISTANCE_METERS = 10;
const MAX_BRIDGE_TARGET_DISTANCE_METERS = 27.5;
const MIN_WALL_TO_AIRCRAFT_PHOTO_SPAN_METERS = 18;
const MAX_WALL_TO_AIRCRAFT_PHOTO_SPAN_METERS = 50;

let source = fs.readFileSync(sourcePath, "utf8");
if (!source.includes(PRIOR_AUTHORITY)) {
  throw new Error(`${sourcePath}: ${PRIOR_AUTHORITY} must run before remote-Rotunda placement`);
}

const startToken = `  // ${PRIOR_AUTHORITY}`;
const endToken = "  // a1-aircraft-target-follows-intact-parent-relocation-v1";
const start = source.indexOf(startToken);
const end = source.indexOf(endToken, start + startToken.length);
if (start < 0 || end < 0 || end <= start) {
  throw new Error(`${sourcePath}: photo source-pose block anchors are missing`);
}

const replacement = `  // ${PRIOR_AUTHORITY}\n  // ${AUTHORITY}\n  // ${SOURCE_REACH_AUTHORITY}\n  // The Aug. 15 overhead photo is visual authority over the old compact source\n  // registration: A1's round Rotunda is genuinely remote from the Terminal 4\n  // facade. Start from the decoded KPHX pose and preserve the COMPLETE supplied\n  // Airport_Jetway.glb hierarchy, then translate that complete parent only to the\n  // photo-derived remote Rotunda position. The terminal and aircraft never move.\n  const rawBglPlacementX = Number(placement.x);\n  const rawBglPlacementZ = Number(placement.z);\n  const rawBglPlacementYaw = Number(placement.yaw);\n  if (![rawBglPlacementX, rawBglPlacementZ, rawBglPlacementYaw].every(Number.isFinite)) {\n    throw new Error("A1 decoded KPHX BGL source pose is missing");\n  }\n  anchor.position.x = rawBglPlacementX;\n  anchor.position.z = rawBglPlacementZ;\n  anchor.rotation.y = Number(placement.yaw);\n  anchor.updateMatrix();\n  group.updateWorldMatrix(true, true);\n  fleet.updateWorldMatrix(true, true);\n  model.updateWorldMatrix(true, true);\n\n  const decodedSourceBridgeDirection = new THREE.Vector3(\n    Math.sin(Number(placement.yaw)),\n    0,\n    Math.cos(Number(placement.yaw)),\n  ).normalize();\n  const sourceAxisRotundaBefore = objectCenterInFleet(THREE, fleet, rotunda);\n  const sourceAxisTunnelBefore = objectCenterInFleet(THREE, fleet, tunnelA);\n  const sourceAxisBridgeDirectionBefore = sourceAxisTunnelBefore.clone().sub(sourceAxisRotundaBefore).setY(0);\n  if (sourceAxisBridgeDirectionBefore.lengthSq() < 0.25) throw new Error("A1 supplied Rotunda-to-Tunnel-A source axis is degenerate");\n  sourceAxisBridgeDirectionBefore.normalize();\n  const sourceAxisCurrentHeading = Math.atan2(sourceAxisBridgeDirectionBefore.x, sourceAxisBridgeDirectionBefore.z);\n  const sourceAxisDesiredHeading = Math.atan2(decodedSourceBridgeDirection.x, decodedSourceBridgeDirection.z);\n  const sourceAxisYawDelta = wrappedAngle(THREE, sourceAxisDesiredHeading - sourceAxisCurrentHeading);\n  anchor.rotation.y += sourceAxisYawDelta;\n  anchor.updateMatrix();\n  group.updateWorldMatrix(true, true);\n  fleet.updateWorldMatrix(true, true);\n  model.updateWorldMatrix(true, true);\n\n  const measuredWallX = Number(group.userData.uploadedJetwayA1MeasuredTerminalWallX);\n  const measuredWallZ = Number(group.userData.uploadedJetwayA1MeasuredTerminalWallZ);\n  const rawTargetXForRotunda = Number(placement.targetX);\n  const rawTargetZForRotunda = Number(placement.targetZ);\n  if (![measuredWallX, measuredWallZ, rawTargetXForRotunda, rawTargetZForRotunda].every(Number.isFinite)) {\n    throw new Error("A1 photo remote-Rotunda wall/aircraft endpoints are missing");\n  }\n  const wallReference = new THREE.Vector3(measuredWallX, 0, measuredWallZ);\n  const aircraftReference = new THREE.Vector3(rawTargetXForRotunda, 0, rawTargetZForRotunda);\n  const wallToAircraft = aircraftReference.clone().sub(wallReference).setY(0);\n  const wallToAircraftMeters = wallToAircraft.length();\n  // The exact BGATE1 facade identity lock moves the terminal endpoint onto the\n  // actual A1 facade and yields a current fixed wall-to-aircraft span of ~48.65 m.\n  // Keep a bounded fail-closed envelope while allowing that real source-derived\n  // endpoint; this changes no terminal, aircraft, Rotunda, or supplied GLB pose.\n  if (!(wallToAircraftMeters > ${MIN_WALL_TO_AIRCRAFT_PHOTO_SPAN_METERS} && wallToAircraftMeters < ${MAX_WALL_TO_AIRCRAFT_PHOTO_SPAN_METERS})) {\n    throw new Error(\`A1 wall-to-aircraft photo span is invalid: \${wallToAircraftMeters}\`);\n  }\n  wallToAircraft.normalize();\n  const wallSide = new THREE.Vector3(wallToAircraft.z, 0, -wallToAircraft.x).normalize();\n\n  const sourceRotundaBeforePhotoPlacement = objectCenterInFleet(THREE, fleet, rotunda);\n  const sourceFromWall = sourceRotundaBeforePhotoPlacement.clone().sub(wallReference).setY(0);\n  const sourceSideProjection = sourceFromWall.dot(wallSide);\n  const photoSideSign = sourceSideProjection < 0 ? -1 : 1;\n  // Solve the Rotunda against the actual connected reach of the exact supplied\n  // Airport_Jetway.glb. The same-head browser trace reports ~26.243 m source reach\n  // and ~26.215 m target reach; the retired 18-22.5 m envelope pulled the Rotunda\n  // too far aircraft-side and collapsed the rendered Rotunda-to-Cab separation.\n  // Keep the visible Aug. 15 dogleg lateral offset, but leave ~25.9-26.4 m for the\n  // intact movable bridge so the telescoping body, not the Rotunda, does the reach.\n  const photoLateralMeters = THREE.MathUtils.clamp(wallToAircraftMeters * 0.10, 2.2, 3.2);\n  const desiredBridgeReachMeters = THREE.MathUtils.clamp(wallToAircraftMeters * 0.54, 25.9, 26.4);\n  const desiredAxialAircraftReachMeters = Math.sqrt(Math.max(\n    1,\n    desiredBridgeReachMeters * desiredBridgeReachMeters - photoLateralMeters * photoLateralMeters,\n  ));\n  const unconstrainedPhotoAlongMeters = wallToAircraftMeters - desiredAxialAircraftReachMeters;\n  const maximumAlongFromWallEnvelope = Math.sqrt(Math.max(\n    1,\n    ${MAX_DIRECT_WALL_DISTANCE_METERS} * ${MAX_DIRECT_WALL_DISTANCE_METERS} - photoLateralMeters * photoLateralMeters,\n  ));\n  const minimumAlongFromWallEnvelope = Math.sqrt(Math.max(\n    1,\n    ${MIN_DIRECT_WALL_DISTANCE_METERS} * ${MIN_DIRECT_WALL_DISTANCE_METERS} - photoLateralMeters * photoLateralMeters,\n  ));\n  const photoAlongMeters = THREE.MathUtils.clamp(\n    unconstrainedPhotoAlongMeters,\n    minimumAlongFromWallEnvelope + 0.35,\n    maximumAlongFromWallEnvelope - 0.35,\n  );\n  const photoRotundaTarget = wallReference.clone()\n    .addScaledVector(wallToAircraft, photoAlongMeters)\n    .addScaledVector(wallSide, photoSideSign * photoLateralMeters);\n  photoRotundaTarget.y = sourceRotundaBeforePhotoPlacement.y;\n  const photoRotundaTranslationX = photoRotundaTarget.x - sourceRotundaBeforePhotoPlacement.x;\n  const photoRotundaTranslationZ = photoRotundaTarget.z - sourceRotundaBeforePhotoPlacement.z;\n  anchor.position.x += photoRotundaTranslationX;\n  anchor.position.z += photoRotundaTranslationZ;\n  anchor.updateMatrix();\n  group.updateWorldMatrix(true, true);\n  fleet.updateWorldMatrix(true, true);\n  model.updateWorldMatrix(true, true);\n\n  let fixedRotundaCenter = objectCenterInFleet(THREE, fleet, rotunda);\n  const photoRotundaPlacementErrorMeters = Math.hypot(\n    fixedRotundaCenter.x - photoRotundaTarget.x,\n    fixedRotundaCenter.z - photoRotundaTarget.z,\n  );\n  if (photoRotundaPlacementErrorMeters > 0.002) {\n    throw new Error(\`A1 complete-parent remote Rotunda placement failed: \${photoRotundaPlacementErrorMeters}\`);\n  }\n  const sourceRotundaTarget = fixedRotundaCenter.clone();\n  const rotatedSourceHeadingRotundaCenter = fixedRotundaCenter.clone();\n\n  const sourceAxisTunnelAfter = objectCenterInFleet(THREE, fleet, tunnelA);\n  const sourceAxisBridgeDirectionAfter = sourceAxisTunnelAfter.clone().sub(fixedRotundaCenter).setY(0);\n  if (sourceAxisBridgeDirectionAfter.lengthSq() < 0.25) throw new Error("A1 final supplied Rotunda-to-Tunnel-A source axis is degenerate");\n  sourceAxisBridgeDirectionAfter.normalize();\n  const sourceAxisAlignmentCosine = sourceAxisBridgeDirectionAfter.dot(decodedSourceBridgeDirection);\n  if (!(sourceAxisAlignmentCosine >= 0.999)) {\n    throw new Error(\`A1 complete-parent bridge axis changed during remote Rotunda placement: cosine=\${sourceAxisAlignmentCosine}\`);\n  }\n  const sourceRotundaPositionErrorMeters = photoRotundaPlacementErrorMeters;\n  const fixedWallPoint = new THREE.Vector3(measuredWallX, fixedRotundaCenter.y, measuredWallZ);\n  const terminalDirection = fixedWallPoint.clone().sub(fixedRotundaCenter).setY(0);\n  let terminalWallDistance = terminalDirection.length();\n  if (!(terminalWallDistance >= ${MIN_DIRECT_WALL_DISTANCE_METERS} && terminalWallDistance <= ${MAX_DIRECT_WALL_DISTANCE_METERS})) {\n    throw new Error(\`A1 photo Rotunda is not genuinely remote from Terminal 4: \${terminalWallDistance}\`);\n  }\n  terminalDirection.normalize();\n  const photoBridgeTargetDistanceMeters = Math.hypot(rawTargetXForRotunda - fixedRotundaCenter.x, rawTargetZForRotunda - fixedRotundaCenter.z);\n  if (!(photoBridgeTargetDistanceMeters >= ${MIN_BRIDGE_TARGET_DISTANCE_METERS} && photoBridgeTargetDistanceMeters <= ${MAX_BRIDGE_TARGET_DISTANCE_METERS})) {\n    throw new Error(\`A1 remote Rotunda leaves an invalid movable-bridge reach: \${photoBridgeTargetDistanceMeters}\`);\n  }\n  group.userData.uploadedJetwayA1RealPhotoGeometryAuthority = "${PRIOR_AUTHORITY}";\n  group.userData.uploadedJetwayA1PhotoRemoteRotundaPlacementAuthority = "${AUTHORITY}";\n  group.userData.uploadedJetwayA1PhotoSourceReachAuthority = "${SOURCE_REACH_AUTHORITY}";\n  group.userData.uploadedJetwayA1RemoteSourceRotunda = true;\n  group.userData.uploadedJetwayA1LongFixedTerminalCorridor = true;\n  group.userData.uploadedJetwayA1PhotoRemoteRotundaWallDistanceMeters = terminalWallDistance;\n  group.userData.uploadedJetwayA1PhotoRemoteRotundaBridgeReachMeters = photoBridgeTargetDistanceMeters;\n  group.userData.uploadedJetwayA1PhotoRemoteRotundaTranslationX = photoRotundaTranslationX;\n  group.userData.uploadedJetwayA1PhotoRemoteRotundaTranslationZ = photoRotundaTranslationZ;\n  group.userData.uploadedJetwayA1PhotoRotundaAlongWallAircraftMeters = photoAlongMeters;\n  group.userData.uploadedJetwayA1PhotoRotundaLateralMeters = photoLateralMeters;\n  group.userData.uploadedJetwayA1PhotoDesiredBridgeReachMeters = desiredBridgeReachMeters;\n  group.userData.uploadedJetwayA1SourceAxisAlignmentAuthority = "a1-intact-parent-source-axis-alignment-v1";\n  group.userData.uploadedJetwayA1SourceAxisAlignmentCosine = sourceAxisAlignmentCosine;\n  group.userData.uploadedJetwayA1SourceAxisYawDeltaRadians = sourceAxisYawDelta;\n\n`;
source = `${source.slice(0, start)}${replacement}${source.slice(end)}`;

// The aircraft is photo/airport authority and must remain fixed. Rewrite only the
// generated target block itself. Do not depend on whatever statement happens to
// follow it; late preparers legitimately rename/restructure the next bridge-axis
// declaration while preserving this target contract.
const targetStart = source.indexOf(endToken);
const targetNormalizeToken = "  targetDirection.normalize();";
const targetNormalizeStart = source.indexOf(targetNormalizeToken, targetStart);
const targetEnd = targetNormalizeStart < 0 ? -1 : targetNormalizeStart + targetNormalizeToken.length;
if (targetStart < 0 || targetEnd < 0 || targetEnd <= targetStart) {
  throw new Error(`${sourcePath}: aircraft target compatibility block anchors are missing`);
}
const targetReplacement = `  // a1-aircraft-target-follows-intact-parent-relocation-v1\n  // ${AUTHORITY}: the aircraft remains at its real gate pose. Do not translate\n  // the target with the corrected jetway parent.\n  const rawTargetX = Number(placement.targetX);\n  const rawTargetZ = Number(placement.targetZ);\n  if (![rawTargetX, rawTargetZ].every(Number.isFinite)) throw new Error("A1 source parking door target is missing");\n  const sourceModelOriginRelocationX = 0;\n  const sourceModelOriginRelocationZ = 0;\n  const sourceModelOriginRelocationMeters = 0;\n  const targetPoint = new THREE.Vector3(rawTargetX, fixedRotundaCenter.y, rawTargetZ);\n  const rawTargetOffsetFromModelOrigin = Math.hypot(rawTargetX - rawBglPlacementX, rawTargetZ - rawBglPlacementZ);\n  const relocatedTargetOffsetFromModelOrigin = rawTargetOffsetFromModelOrigin;\n  const aircraftTargetFrameErrorMeters = 0;\n  const targetDirection = targetPoint.clone().sub(fixedRotundaCenter);\n  targetDirection.y = 0;\n  const targetDistance = targetDirection.length();\n  if (!(targetDistance >= ${MIN_BRIDGE_TARGET_DISTANCE_METERS} && targetDistance <= ${MAX_BRIDGE_TARGET_DISTANCE_METERS})) {\n    throw new Error(\`A1 fixed aircraft target is outside the remote-Rotunda movable bridge envelope: \${targetDistance}\`);\n  }\n  targetDirection.normalize();`;
source = `${source.slice(0, targetStart)}${targetReplacement}${source.slice(targetEnd)}`;

for (const required of [
  AUTHORITY,
  SOURCE_REACH_AUTHORITY,
  "const photoRotundaTarget = wallReference.clone()",
  "const desiredBridgeReachMeters = THREE.MathUtils.clamp(wallToAircraftMeters * 0.54, 25.9, 26.4);",
  "const unconstrainedPhotoAlongMeters = wallToAircraftMeters - desiredAxialAircraftReachMeters;",
  "anchor.position.x += photoRotundaTranslationX;",
  "anchor.position.z += photoRotundaTranslationZ;",
  `wallToAircraftMeters > ${MIN_WALL_TO_AIRCRAFT_PHOTO_SPAN_METERS}`,
  `wallToAircraftMeters < ${MAX_WALL_TO_AIRCRAFT_PHOTO_SPAN_METERS}`,
  `terminalWallDistance >= ${MIN_DIRECT_WALL_DISTANCE_METERS}`,
  `terminalWallDistance <= ${MAX_DIRECT_WALL_DISTANCE_METERS}`,
  "uploadedJetwayA1PhotoRemoteRotundaPlacementAuthority",
  "uploadedJetwayA1PhotoSourceReachAuthority",
  "const sourceModelOriginRelocationX = 0;",
  "const targetPoint = new THREE.Vector3(rawTargetX, fixedRotundaCenter.y, rawTargetZ);",
]) {
  if (!source.includes(required)) throw new Error(`${sourcePath}: remote A1 Rotunda output is missing ${required}`);
}

fs.writeFileSync(sourcePath, source, "utf8");
console.log(`Prepared ${AUTHORITY} + ${SOURCE_REACH_AUTHORITY}: moved only the complete exact A1 jetway parent to a genuinely remote photo-derived Rotunda, kept Terminal 4 and the aircraft fixed, preserved every supplied child transform, accepted the measured long BGATE1-to-aircraft span within the bounded 18-50 m photo envelope, and reserved the exact supplied connected ~26.24 m reach for the movable bridge instead of the retired 18-22.5 m compact envelope.`);