// Deprecated by the Aug. 15 KPHX photo-authoritative Gate A1 repair.
//
// This pass previously moved the complete supplied A1 parent so the Rotunda
// ended only 2.4 m from a generic terminal wall and then built a compact white
// vestibule. The real Gate A1 photographs establish the opposite geometry: the
// actual Terminal 4/BGATE1 facade feeds a long elevated fixed corridor, a
// dogleg/elbow, and a remote round Rotunda. The supplied movable jetway starts
// at that remote Rotunda and must not be translated toward the wall to create a
// short sleeve.
//
// Keep this legacy path as a compatibility no-op. The later BGATE1
// photo-authoritative wall/dogleg/remote-Rotunda stages own A1. A3 and the
// subsequent A gates keep their independent short/direct connector geometry.

console.log(
  "Skipped retired 2.4 m A1 photo-registered stop; Aug. 15 BGATE1 long fixed corridor/dogleg/remote-Rotunda authority remains intact.",
);
