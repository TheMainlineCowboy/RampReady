import fs from "node:fs";

const runtimePath = "src/environment/registerStaticJetwayFleetToFacadeV1.js";
const marker = "static-a12-articulation-exact-envelope-sweep-v2";
let source = fs.readFileSync(runtimePath, "utf8");

if (!source.includes(marker)) {
  const oldBlock = `  if (staticExactPartOverlaps.length) {\n    throw new Error(\`Static Terminal 4 exact supplied part envelopes overlap after final registration/telescoping: \${staticExactPartOverlaps.join(", ")}\`);\n  }`;
  const newBlock = `  if (staticExactPartOverlaps.length) {\n    // ${marker}\n    const staticPartNameForBatch = (batch) => {\n      const explicit = batch.userData?.sourcePartName;\n      if (explicit) return explicit;\n      const name = String(batch.name || \"\");\n      if (/Rotunda/i.test(name)) return \"Rotunda\";\n      if (/Tunnel_A/i.test(name)) return \"Tunnel_A\";\n      if (/Tunnel_B/i.test(name)) return \"Tunnel_B\";\n      if (/Tunnel_C/i.test(name)) return \"Tunnel_C\";\n      if (/Cab/i.test(name)) return \"Cab\";\n      return name;\n    };\n    const a12Index = staticRegisteredPlacements.findIndex((placement) => placement.gate === \"A12\");\n    const a14Index = staticRegisteredPlacements.findIndex((placement) => placement.gate === \"A14\");\n    if (a12Index < 0 || a14Index < 0) throw new Error(\"A12/A14 articulation sweep could not resolve static instance indices\");\n    const a12Registered = staticRegisteredPlacements[a12Index];\n    const a12Original = staticOriginalPlacements[a12Index];\n    const movableA12Batches = staticBatches.filter((batch) => staticPartNameForBatch(batch) !== \"Rotunda\");\n    const baselineA12Matrices = movableA12Batches.map((batch) => {\n      const matrix = new THREE.Matrix4();\n      batch.getMatrixAt(a12Index, matrix);\n      return { batch, matrix: matrix.clone(), part: staticPartNameForBatch(batch) };\n    });\n    const rotateA12Arm = (deltaRadians) => {\n      const px = Number(a12Registered.x);\n      const pz = Number(a12Registered.z);\n      const toOrigin = new THREE.Matrix4().makeTranslation(-px, 0, -pz);\n      const rotation = new THREE.Matrix4().makeRotationY(deltaRadians);\n      const fromOrigin = new THREE.Matrix4().makeTranslation(px, 0, pz);\n      const aboutPivot = new THREE.Matrix4().multiplyMatrices(fromOrigin, rotation).multiply(toOrigin);\n      const next = new THREE.Matrix4();\n      for (const { batch, matrix } of baselineA12Matrices) {\n        next.multiplyMatrices(aboutPivot, matrix);\n        batch.setMatrixAt(a12Index, next);\n      }\n    };\n    const restoreA12Arm = () => {\n      for (const { batch, matrix } of baselineA12Matrices) batch.setMatrixAt(a12Index, matrix);\n      for (const batch of movableA12Batches) batch.instanceMatrix.needsUpdate = true;\n    };\n    const measureA12Candidate = (degrees) => {\n      rotateA12Arm(THREE.MathUtils.degToRad(degrees));\n      const candidateParts = staticBatches.map((batch) => staticExactInstanceEnvelope(THREE, batch, a12Index));\n      const overlaps = [];\n      let maximumDepth = 0;\n      let totalDepth = 0;\n      for (let otherIndex = 0; otherIndex < finalExactEnvelopes.length; otherIndex += 1) {\n        if (otherIndex === a12Index) continue;\n        const otherGate = finalExactEnvelopes[otherIndex];\n        for (const leftPart of candidateParts) {\n          for (const rightPart of otherGate.parts) {\n            const depth = staticEnvelopeOverlapDepthXZ(leftPart, rightPart);\n            if (depth <= ${0.05}) continue;\n            maximumDepth = Math.max(maximumDepth, depth);\n            totalDepth += depth;\n            overlaps.push(\`A12/\${leftPart.part}<->\${otherGate.gate}/\${rightPart.part}=\${depth.toFixed(3)}m\`);\n          }\n        }\n      }\n      return { degrees, overlapCount: overlaps.length, maximumDepth, totalDepth, overlaps: overlaps.slice(0, 8) };\n    };\n    const sourceDeltaDegrees = THREE.MathUtils.radToDeg(wrapYaw(THREE, Number(a12Original.yaw) - Number(a12Registered.yaw)));\n    const candidateDegrees = [];\n    for (let degrees = -40; degrees <= 40; degrees += 1) candidateDegrees.push(degrees);\n    candidateDegrees.push(sourceDeltaDegrees);\n    const uniqueCandidates = [...new Set(candidateDegrees.map((degrees) => Number(degrees.toFixed(6))))];\n    const sweepResults = uniqueCandidates.map(measureA12Candidate);\n    restoreA12Arm();\n    const rankedResults = [...sweepResults].sort((left, right) =>\n      left.overlapCount - right.overlapCount\n      || left.maximumDepth - right.maximumDepth\n      || Math.abs(left.degrees - sourceDeltaDegrees) - Math.abs(right.degrees - sourceDeltaDegrees)\n      || Math.abs(left.degrees) - Math.abs(right.degrees));\n    const collisionFree = rankedResults.filter((result) => result.overlapCount === 0).slice(0, 12);\n    const staticOverlapAnchorDiagnostics = [\"A12\", \"A14\"].map((gate) => {\n      const registered = staticRegisteredPlacements.find((placement) => placement.gate === gate) || {};\n      const original = staticOriginalPlacements.find((placement) => placement.gate === gate) || {};\n      return {\n        gate,\n        source: { x: Number(original.x), z: Number(original.z), yaw: Number(original.yaw) },\n        rotunda: { x: Number(registered.x), z: Number(registered.z) },\n        wall: { x: Number(registered.staticFacadeWallX), z: Number(registered.staticFacadeWallZ) },\n        target: { x: Number(registered.targetX), z: Number(registered.targetZ) },\n        modelRoot: { x: Number(registered.staticModelRootX), z: Number(registered.staticModelRootZ) },\n        wallNormal: { x: Number(registered.staticCornerWallNormalX), z: Number(registered.staticCornerWallNormalZ) },\n        finalContactDistance: Number(registered.staticPostRegistrationPredictedContactDistanceMeters),\n        yaw: Number(registered.yaw),\n      };\n    });\n    throw new Error(\`Static Terminal 4 exact supplied part envelopes overlap after final registration/telescoping: \${staticExactPartOverlaps.join(", ")}; ${marker} sourceDeltaDegrees=\${sourceDeltaDegrees.toFixed(3)} collisionFree=\${JSON.stringify(collisionFree)} best=\${JSON.stringify(rankedResults.slice(0, 12))} anchors=\${JSON.stringify(staticOverlapAnchorDiagnostics)}\`);\n  }`;
  if (!source.includes(oldBlock)) {
    throw new Error(`${runtimePath}: exact static overlap failure block is missing before A12 articulation sweep`);
  }
  source = source.replace(oldBlock, newBlock);
}

for (const required of [
  marker,
  "movableA12Batches",
  "rotateA12Arm",
  "sourceDeltaDegrees",
  "collisionFree",
  "rankedResults",
]) {
  if (!source.includes(required)) throw new Error(`${runtimePath}: A12 articulation sweep is missing ${required}`);
}

fs.writeFileSync(runtimePath, source, "utf8");
console.log("Armed a geometry-neutral A12 Rotunda articulation sweep: exact Tunnel A/B/C/Cab envelopes are tested against all 56 other static jetways, then restored before failure reporting.");
