{
  "name": "rampready",
  "version": "0.1.6",
  "private": true,
  "description": "RampReady pushback training simulator prototype.",
  "type": "module",
  "scripts": {
    "prepare:runtime": "node scripts/prepare-runtime.mjs",
    "dev": "npm run prepare:runtime && vite --host 0.0.0.0",
    "build": "npm run prepare:runtime && npm run verify && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "verify": "node scripts/verify-rampready.mjs && node scripts/verify-physics.mjs",
    "build:checked": "npm run build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "three": "^0.171.0"
  },
  "devDependencies": {}
}
